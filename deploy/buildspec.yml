version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
  build:
    commands:
      - echo "Validating Python syntax"
      - python -m compileall src
      - cp deploy/codedeploy/appspec.yml appspec.yml

artifacts:
  files:
    - '**/*'

